apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "demo-geocontext.fullname" . }}-test-connection"
  labels:
    {{- include "demo-geocontext.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "demo-geocontext.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
